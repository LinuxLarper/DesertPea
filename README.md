# DesertPea

you need to get npm, node and download requirments using the json
To run use node index.js itll run on port 8000, allow the firewall and go to localhost:8000 to do it or set up a new port for it
